# BLOOM BOX 開発引き継ぎ

最終確認：2026-09-14（JST）。コード基準：main `a6c59bee6c5bbfda1b9ce40573fc81195208335f`（PR #101）。この文書はチャット履歴や特定AIのメモリを必要としない入口です。残課題の状態は [BACKLOG.md](BACKLOG.md) に集約します。

## 最初に確認すること

1. リポジトリの [AGENTS.md](../../AGENTS.md) を読み、作業ディレクトリ・remote・branch・差分を確認する。未コミットの利用者ファイルを巻き込まない。
2. この文書と [BACKLOG.md](BACKLOG.md) の「確認済みの状態」「次に進める順序」を読む。対象課題の資料だけ追加で読む。
3. 最新main、対象PRとチェック、本番設定など、変わる状態は必要な範囲で再取得する。記録の日付を現在の確認結果へ置き換えない。
4. ローカルURLのポートだけでアプリを判断しない。この作業履歴では `localhost:3000` をwaricaが使用していた。`localhost:3012` はBLOOM BOXの確認用だったが、将来の稼働は保証しない。サーバーの作業ディレクトリと実際の画面を確認してから操作する。
5. 課題を1つに絞り、既存実装・完了条件・必要な検証を確認して着手する。実装済みの基盤を作り直さない。

## 現在の方針と確定事項

- [ADR 0009](../architecture/adr/0009-native-commerce-and-google-customers.md) が現行方針。顧客・商品・価格・在庫・注文・発送をBloomBoxのPostgreSQLで管理し、Googleで直接顧客認証する。顧客認証と運営者認証は別クライアント・秘密値・Cookie・権限。
- 次の決済接続先は既存のStripe Checkout。カード番号はBloomBoxで扱わず、署名検証済みの通知と照合で支払を確定する。Stripeの商用契約・販売開始が承認済みという意味ではない。
- Shopifyは移行前の実装。古いSDKや文書が残っていても、Shopifyへ戻す根拠にならない。既存取引・資格情報・データを無条件に削除せず、元の決済先を維持する。
- ユーザー確認済み：税込、日本全国配送、M＝商品4,000円＋送料1,000円、L＝商品8,000円＋送料0円、初期は1回1箱。紹介特典の最低商品小計は5,500円。紹介制度はTest Mode専用で、実決済への割引接続は未完了。
- 箱は縦長の方向性。M/Lの寸法・花材・本数・内容量・梱包仕様は未確定。画像はパッケージ案であり、実物の証拠ではない。低送料に収めたい意向はあるが、数値や配送費を推測で確定しない。
- ギフトの新規初期値は日本時間の今日＋3日と「いつもありがとう」。利用者の編集値は保持する。アクセントはミントのグラデーション。デザインの詳細は [DESIGN_SYSTEM.md](../design/DESIGN_SYSTEM.md)。

## 実装済みと実接続の境界

| 対象 | 確認できている範囲 | まだ確認していない範囲 |
| --- | --- | --- |
| 顧客Google・注文履歴 | ログイン、履歴・詳細、所有権確認。実Googleの2顧客と架空注文で相互非表示を確認 | ブラウザーで認証済み購入→実Stripeテスト決済→本人の注文履歴、本番HTTPS |
| 商品・在庫管理 | 画面・権限・履歴。実Googleと隔離DBで登録・編集・在庫訂正・競合・失効を確認 | 正式な商品画像・実商品・実在庫の本番登録、送料編集の実Google経由の確認 |
| 注文・在庫・送料 | 顧客紐付け、価格・送料固定、在庫予約・確定・安全な解放、重複・失敗処理を単体／DBで確認 | 実Stripe接続、結果不明の取引の期限後復旧、発送・返品の一連の運用 |
| 購入画面 | M/L、カート編集、初期値、保存失敗からの復旧、スマホの横ずれ抑制 | 商用決済全体のブラウザー試験、実機Safari・読み上げを含む公開前確認 |

根拠：[2顧客の表示分離](CUSTOMER_ORDER_ISOLATION_VERIFICATION.md)、[商品管理接続](NATIVE_CATALOG_CONNECTION_VERIFICATION.md)、[在庫](NATIVE_INVENTORY.md)、[送料](PURCHASE_SHIPPING.md)、[画面検証](../design/VERIFICATION.md)。古い各機能文書の「未確認」は後続の検証記録と照合する。

## 会員ランクと購入履歴（2026-09-14）

ユーザーはランク別自動割引を選択済み。[ADR 0013](../architecture/adr/0013-customer-rank-discounts.md) と [検証・導入記録](CUSTOMER_LOYALTY.md) を参照。履歴の商品表示、全件の確定商品購入実績、0/2/3/5%のランク先行案、割引固定、Stripe照合を実装。0025 migrationが必要。実Google/Stripeの割引E2Eと料率/条件の確定は残る。紹介Test Modeとは別で、本番受付停止を維持する。

## 本番販売は停止中

- [activation設定](../../config/production-commerce-activation.json) は `blocked`、証跡8項目は未完了。[案内コンテンツ](../../content/storefront.json) は `draft`。
- [composition-root](../../src/shared/infrastructure/composition-root.ts) の `acceptsNewCheckout` は本番の新規購入を拒否する。環境変数1つで販売を開始できる状態ではない。
- 2026-09-14の `pnpm check:production` は案内未承認・販売未承認で失敗。正常な停止であり、チェックを通すために承認値だけを変更しない。
- CI成功、ローカル表示、Googleログイン、架空の支払済み注文は、実決済や販売開始の証拠ではない。Commerce Workerも無効設定時に成功終了できるため、実処理の証拠を確認する。
- この引き継ぎは本番公開、実課金、外部アカウント作成、実在庫登録、契約・仕様確定の承認ではない。現在のユーザー指示と既存の承認範囲を確認し、通常の調査・実装・検証は自律的に進める。

## 次の担当者が進める順序

1. P0-05/10/18：本番停止を保つ隔離Stripeテスト環境と、認証→商品→在庫→決済→注文表示の経路をそろえる。鍵をチャットに貼らせず、設定先・必要権限・検証手順を具体化する。外部設定待ちの間もコードと検証準備を進める。
2. P0-14/15：自作注文の発送・追跡・通知を完成。初期は権限付き手動登録や事業者機能で完了条件を満たせるなら過剰な基盤を作らない。
3. P0-07/08/16/17/23：商品・配送・返品・窓口・個人情報の条件を確定し、運用と表示に反映。
4. P0-01/02/11/12/20/22：本番権限・設定・復旧演習を整備。main保護と必須承認は並行して進める。
5. P0-21：証跡と対象SHAをそろえ、[RELEASE.md](RELEASE.md) に沿って最終承認・公開確認へ進む。

## 作業後に残すもの

- PRタイトル・本文・レビューコメントは日本語。機械判定が必要なテンプレート見出し・IDは保持する。
- 対象のBACKLOG行を更新し、実装／判断／外部設定／検証のどこが進んだかを書く。完了は証跡へのリンクと日付を伴い、課題IDは再利用しない。
- 実接続記録には対象SHA、環境の種類、実施範囲、残る範囲を記載。秘密値、実顧客のメール・住所・識別子を公開文書へ書かない。
- 確認待ちを未設定と断定しない。外部設定、PR、ローカルURLの状態は次回も再確認する。詳細をこの文書へ重複させず、正本のBACKLOG・ADR・機能別記録を更新する。
